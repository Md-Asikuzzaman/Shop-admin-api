import Layout from '@/components/layout/layout';
import { NextPage } from 'next';

interface Props {}

const Customer: NextPage<Props> = ({}) => {
  return <Layout>customer page</Layout>;
};

export default Customer;
